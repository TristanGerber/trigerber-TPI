<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
    xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
    x:Class="GestTask.Views.TaskMenuPopup"
    xmlns:local="clr-namespace:GestTask.ViewModels">
    <!--You can set an animation in the xaml file or in the csharp code behind-->
    <pages:PopupPage.Animation>
        <animations:ScaleAnimation 
            PositionIn="Center"
            PositionOut="Center"
            ScaleIn="1.2"
            ScaleOut="0.8"
            DurationIn="400"
            DurationOut="300"
            EasingIn="SinOut"
            EasingOut="SinIn"
            HasBackgroundAnimation="True"/>
    </pages:PopupPage.Animation>
    <!--You can use any elements here which are extended from Xamarin.Forms.View-->
    <StackLayout x:DataType="local:TaskMenuViewModel"
        BackgroundColor="LightGoldenrodYellow"
        VerticalOptions="Center"
        HorizontalOptions="Center"
        Padding="80, 20, 80, 20">
        <Entry Text="{Binding Name}" Placeholder="Nom de la tâche"/>
        <Entry Text="{Binding Description}" Placeholder="Description"/>
        <Label Text="Active"/>
        <CheckBox x:Name="ActiveCheckBox" IsChecked="{Binding Active}"/>
        <Label Text="Date d'échéance"/>
        <DatePicker Date="{Binding PassingDate}"/>
        <Label Text="Dans la ToDoList"/>
        <CheckBox IsChecked="{Binding InToDoList}"/>
        <Label Text="Catégorie"/>
        <Picker SelectedIndex="{Binding FkCategory}"  Title="Catégorie"/>
        <Button Text="Sauvegarder" Command="{Binding Source={RelativeSource AncestorType={x:Type local:TaskMenuViewModel}}, Path=SaveCommand}"/>
        <Button Text="Supprimer" Command="{Binding Source={RelativeSource AncestorType={x:Type local:TaskMenuViewModel}}, Path=DeleteCommand}"/>
        <Button Text="Annuler" Command="{Binding Source={RelativeSource AncestorType={x:Type local:TaskMenuViewModel}}, Path=CancelCommand}"/>
    </StackLayout>
</pages:PopupPage>